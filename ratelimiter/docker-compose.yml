services:
  server:
    build: ./server
  ratelimiter:
    build: ./ratelimiter
    ports:
      - 3001:3001
    environment:
      ALGO: fixed_window
      LIMIT: 3
      RATE: 3
  cache:
    image: redis:7.4-alpine
